<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="10dp"
    >
    
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/passenger_image_item_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:background="@drawable/null_icon"
       />
    <TextView
        android:id="@+id/passenger_image_item_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/passenger_image_item_image"
        app:layout_constraintStart_toStartOf="@id/passenger_image_item_image"
        app:layout_constraintEnd_toEndOf="@id/passenger_image_item_image"
        android:text="Tyler"
        android:layout_marginTop="10dp"
        style="@style/input"

        />

</androidx.constraintlayout.widget.ConstraintLayout>