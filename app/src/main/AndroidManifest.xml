<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fellow_traveller">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".Models.GlobalClass"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapDirections.MapsActivity"
            android:label="@string/title_activity_maps"></activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".MapDirections.MapsRouteActivity" />
        <activity android:name=".SearchAndBook.SearchPassengersActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CollapseTest" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".Reviews.WriteReviewActivity" />
        <activity android:name=".Reviews.ReviewsActivity" />
        <activity android:name=".NewOffer.AddLocationActivity" />
        <activity android:name=".Settings.EditCarSettingsActivity" />
        <activity android:name=".Settings.AddCarSettingsActivity" />
        <activity android:name=".Settings.CarsSettingsActivity" />
        <activity android:name=".SearchAndBook.BookActivity" />
        <activity android:name=".SearchAndBook.SearchDetailsActivity" />
        <activity android:name=".SearchAndBook.SearchResultsActivity" />
        <activity android:name=".SearchAndBook.Search2Activity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".SearchAndBook.SearchActivity" />
        <activity android:name=".NewOffer.NewOfferActivity" />
        <activity android:name=".SearchAndBook.FiltersActivity" />
        <activity android:name=".Settings.PersonalSettingsActivity" />
        <activity android:name=".Chat.ChatConversationActivity" />
        <activity android:name=".Settings.UserSettingsActivity" />
        <activity android:name=".Trips.TripPageActivity" />
        <activity android:name=".Register.RegisterContainerActivity" />
        <activity android:name=".HomeFragments.HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Settings.ChangeUserPasswordActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <service
            android:name=".MessagesNotification.MyFirebaseIdService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".MessagesNotification.MyFirebaseMessaging"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".Models.ExampleJobService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>